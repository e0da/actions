# Codenames

Each release may have a human-readable codename. The name is deterministic from
the source repository and normalized source tag, using the centrally maintained
codename data, so retries always compose the same release title.

Codenames are presentation metadata. They do not alter SemVer, tag identity,
asset names, source provenance, access scope, or the generated PR summary.
